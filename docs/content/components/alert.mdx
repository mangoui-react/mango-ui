---
title: Alert
prevTitle: 'Accordion'
prevSlug: '/components/accordion'
nextTitle: 'Avatar'
nextSlug: '/components/avatar'
---

# Alert

<Description>Displays a description for the user's attention.</Description>

<DemoContainer>
  <AlertDemo />
  <DemoCodeBlock component="alert" />
</DemoContainer>

## Installation

<PackageManagers
  command={{
    npm: 'npm i @mangoui/alert',
    yarn: 'yarn add @mangoui/alert',
    pnpm: 'pnpm add @mangoui/alert',
    bun: 'bun add @mangoui/alert',
  }}
/>

## Import

<ImportSyntax
  command={{
    individual: `import { AlertContent, AlertRoot, AlertStatusIcon } from '@mangoui/alert';`,
    global: `import { AlertContent, AlertRoot, AlertStatusIcon } from '@mangoui/react';`,
  }}
/>

- **AlertRoot**: All parts of the alert are included.
- **AlertStatusIcon**: Alert icon changes depending on status.
- **AlertContent**: The content of the alert to be announced by screen readers.

### Another way to import

```js
'use client';

import { Alert } from '@mangoui/alert';

<Alert.Root>
  <Alert.StatusIcon />
  <Alert.Content>Content1</Alert.Content>
</Alert.Root>;
```

> 'use client' must be used when rendering on the server side.

## API Reference

### AlertRoot

All parts of the alert are included.

<PropsTable
  data={[
    {
      name: 'status',
      required: false,
      default: '"info"',
      type: (
        <>
          TypeAttributes.Status
          <br />
          <br />
          "info" | "success" | "warning" | "error"
        </>
      ),
      description: 'The status of the alert.',
    },
  ]}
/>

<DataAttributesTable
  data={[
    {
      attribute: '[data-status]',
      values: '"info" | "success" | "warning" | "error"',
    },
  ]}
/>

### AlertStatusIcon

Alert icon changes depending on status.

<PropsTable
  data={[
    {
      name: 'info',
      required: false,
      default: '<InfoIcon />',
      type: 'React.ReactNode',
      description: 'displaying an icon for info status.',
    },
    {
      name: 'success',
      required: false,
      default: '<SuccessIcon />',
      type: 'React.ReactNode',
      description: 'displaying an icon for success status.',
    },
    {
      name: 'warning',
      required: false,
      default: '<WarningIcon />',
      type: 'React.ReactNode',
      description: 'displaying an icon for warning status.',
    },
    {
      name: 'error',
      required: false,
      default: '<ErrorIcon />',
      type: 'React.ReactNode',
      description: 'displaying an icon for error status.',
    },
  ]}
/>

### AlertContent

The content of the alert to be announced by screen readers.

<DataAttributesTable
  data={[
    {
      attribute: '[data-status]',
      values: '"info" | "success" | "warning" | "error"',
    },
  ]}
/>

## Examples

### Set the status value

```jsx
<Alert.Root status="success">
  <Alert.StatusIcon />
  <Alert.Content>This is an success alert</Alert.Content>
</Alert.Root>
```

### Custom

```jsx
<Alert.Root>
  <Alert.StatusIcon />
  <Alert.Content>
    <div style={{ marginTop: -2, fontWeight: 'bold', marginBottom: '0.35em' }}>information</div>
    <div>This is an info alert</div>
  </Alert.Content>
</Alert.Root>
```
