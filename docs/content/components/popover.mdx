---
title: Popover
prevTitle: 'NumberInput'
prevSlug: '/components/number-input'
nextTitle: 'Progress'
nextSlug: '/components/progress'
---

# Popover

<Description>A popover is a floating non-modal dialog box.</Description>

<DemoContainer>
  <PopoverDemo />
  <DemoCodeBlock component="popover" />
</DemoContainer>

## Installation

<PackageManagers
  command={{
    npm: 'npm i @melio-ui/popover',
    yarn: 'yarn add @melio-ui/popover',
    pnpm: 'pnpm add @melio-ui/popover',
    bun: 'bun add @melio-ui/popover',
  }}
/>

## Import

<ImportSyntax
  command={{
    individual: `import { 
  PopoverRoot,
  PopoverTrigger,
  PopoverPortal,
  PopoverContent,
  PopoverClose,
} from '@melio-ui/popover';`,
    global: `import { 
  PopoverRoot,
  PopoverTrigger,
  PopoverPortal,
  PopoverContent,
  PopoverClose,
} from '@melio-ui/react';`,
  }}
/>

- **PopoverRoot**: All parts of the popover are included.
- **PopoverTrigger**: Used to activate or open the Popover component.
- **PopoverPortal**: Portal the overlay and content portion into the body.
- **PopoverContent**: Includes the content that will be displayed in the open menu box.
- **PopoverClose**: The button that closes the menu.

### Another way to import

```js
'use client';

import { Popover } from '@melio-ui/popover';

<Popover.Root>
  <Popover.Trigger asChild>
    <button type="button">Open Popover</button>
  </Popover.Trigger>
  <Popover.Portal>
    <Popover.Content>
      <div>This is a Popover Content</div>
    </Popover.Content>
  </Popover.Portal>
</Popover.Root>;
```

> 'use client' must be used when rendering on the server side.

## API Reference

### PopoverRoot

All parts of the popover are included.

<PropsTable
  data={[
    {
      name: 'open',
      required: false,
      type: 'boolean',
      description: 'Sets whether the popover content is open or closed.',
    },
    {
      name: 'defaultOpen',
      required: false,
      type: 'boolean',
      description:
        'When first rendered, the popover content is in an opened state. Use when there is no need to control the opened state.',
    },
    {
      name: 'onOpenChange',
      required: false,
      type: '(open: boolean) => void',
      description:
        'This is an event handler that is triggered when the open state of the popover content changes.',
    },
  ]}
/>

### PopoverTrigger

Used to activate or open the Popover component.

<PropsTable
  data={[
    {
      name: 'asChild',
      required: false,
      type: 'boolean',
      description:
        'Changes the default rendering element passed as a child, merging its props and behavior.',
    },
  ]}
/>

<DataAttributesTable
  data={[
    {
      attribute: '[data-state]',
      values: '"open" | "closed"',
    },
  ]}
/>

### PopoverPortal

Portal the overlay and content portion into the body.

<PropsTable
  data={[
    {
      name: 'container',
      required: false,
      default: 'document.body',
      type: 'HTMLElement | (() => HTMLElement)',
      description: 'Specifies the container element to which the content should be portaled.',
    },
  ]}
/>

### PopoverContent

Includes the content that will be displayed in the open menu box.

<PropsTable
  data={[
    {
      name: 'triggerPosition',
      required: false,
      default: '{ vertical: "bottom", horizontal: "left" }',
      type: (
        <>
          TriggerPositionValue
          <br />
          <br />
          {`
            {
              vertical: "top" | "center" | "bottom",
              horizontal: "left" | "center" | "right",
            }
          `}
        </>
      ),
      description: 'The location of the trigger where the menu will appear.',
    },
    {
      name: 'contentPosition',
      required: false,
      type: (
        <>
          TriggerPositionValue
          <br />
          <br />
          {`
            {
              vertical: "top" | "center" | "bottom",
              horizontal: "left" | "center" | "right",
            }
          `}
        </>
      ),
      description: 'The location of the menu content where the menu will appear.',
    },
    {
      name: 'position',
      required: false,
      type: (
        <>
          PositionValue
          <br />
          <br />
          {`
            {
              left: number,
              top: number,
            }
          `}
        </>
      ),
      description: 'Specifies the location where the menu will be displayed using coordinates.',
    },
    {
      name: 'zIndex',
      required: false,
      type: 'number',
      description:
        'A property that controls the stacking order of elements on a web page.(CSS z-index)',
    },
    {
      name: 'destroyOnClose',
      required: false,
      default: 'true',
      type: 'boolean',
      description: 'Used to ensure that the DOM is removed when closed.',
    },
    {
      name: 'forceMount',
      required: false,
      type: 'boolean',
      description:
        "Used to ensure that a component is always rendered in the DOM, regardless of its visibility or whether it's conditionally displayed.",
    },
    {
      name: 'closeOnEsc',
      required: false,
      default: 'true',
      type: 'boolean',
      description: 'Determines whether or not to close when the ESC key is pressed.',
    },
    {
      name: 'closeOnBlur',
      required: false,
      default: 'true',
      type: 'boolean',
      description: 'Determines whether to close on blur.',
    },
  ]}
/>

<DataAttributesTable
  data={[
    {
      attribute: '[data-state]',
      values: '"open" | "closed"',
    },
  ]}
/>

### PopoverClose

The button that closes the menu.

<PropsTable
  data={[
    {
      name: 'asChild',
      required: false,
      type: 'boolean',
      description:
        'Changes the default rendering element passed as a child, merging its props and behavior.',
    },
  ]}
/>

## Examples

### Position of popover content

```jsx
<PopoverRoot>
  <PopoverTrigger asChild>
    <button type="button">Open Popover</button>
  </PopoverTrigger>
  <PopoverPortal>
    <PopoverContent
      triggerPosition={{ vertical: 'bottom', horizontal: 'right' }}
      contentPosition={{ vertical: 'top', horizontal: 'left' }}
    >
      <div style={{ padding: 10 }}>This is a Popover Content</div>
    </PopoverContent>
  </PopoverPortal>
</PopoverRoot>
```

### Click the button to close

```jsx
<PopoverRoot>
  <PopoverTrigger asChild>
    <button type="button">Open Popover</button>
  </PopoverTrigger>
  <PopoverPortal>
    <PopoverContent closeOnBlur={false}>
      <div style={{ padding: 10, display: 'flex', gap: 10, alignItems: 'center' }}>
        This is a Popover Content
        <PopoverClose asChild>
          <button type="button">Close</button>
        </PopoverClose>
      </div>
    </PopoverContent>
  </PopoverPortal>
</PopoverRoot>
```
