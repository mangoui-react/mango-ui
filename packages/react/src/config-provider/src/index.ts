export { default as ConfigProvider } from './ConfigProvider';
export { default as useConfig } from './useConfig';

export * from './ConfigProvider';

// TODO: 필요없어 보임 - 좀 생각해 보고 없애는 쪽으로 하자 (locale은 없앨거고, theme는 필요없을 거 같음 headless 이므로)
